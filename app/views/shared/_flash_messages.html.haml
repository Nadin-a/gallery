- if flash[:error]
  .alert.alert-danger
    %p
      - if flash[:error].is_a? Array
        - flash[:error].each do |err|
          = err
          %br
      - else
        = h flash[:error]
- if flash[:success]
  .alert.alert-success
    %p
      = h flash[:success]