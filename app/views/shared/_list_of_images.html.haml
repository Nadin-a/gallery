- @images.each_slice(3) do |images|
  - images.each do |image|
    .col-sm-4
      %br
        = link_to(image.title, category_image_path(image.category, image), { data:{ toggle:'tooltip',placement:'left'}, title:'Open this image'})
        %br
        = link_to(image_tag(image.picture.thumb.url), category_image_path(image.category, image),{ data:{ toggle:'tooltip',placement:'left'}, title:'Open this image'})
        %br
          - if user_signed_in?
            - if current_user == Category.find(image.category_id).owner
              -#%a{remote: true, "data-target": '#updateImageModal', "data-toggle": 'modal', href: edit_category_image_path(image.category, image)}
              -#  Update
              -#  = #render 'images/edit', locals: {category: image.category, image: image}
              =link_to('Delete', category_image_path(image.category, image), method: :delete, data: { confirm: 'You sure?', toggle:'tooltip',placement:'left'}, title:'Delete this image')
              %br
.col-sm-12
  = will_paginate @images
